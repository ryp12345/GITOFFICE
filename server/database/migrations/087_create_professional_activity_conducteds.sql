-- Migration for professional_activity_conducteds table
CREATE TABLE professional_activity_conducteds (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    egov_id varchar(50) NOT NULL,
    title varchar(255) NOT NULL,
    organizer varchar(255) NOT NULL,
    co_organizer varchar(255),
    level text NOT NULL,
    category text NOT NULL,
    sponsored text,
    sponsoring_agency_name_address varchar(255),
    from_date date NOT NULL,
    to_date date NOT NULL,
    place varchar(255) NOT NULL,
    no_of_days varchar(255) NOT NULL,
    role text,
    validation_status text NOT NULL DEFAULT 'new',
    reason varchar(255),
    document varchar(255) NOT NULL,
    created_at timestamp,
    updated_at timestamp,
    CHECK (level IN ('Local','National','International')),
    CHECK (category IN ('Workshop','Seminar','Webinar','FDP','STTP','MDP/EDP','Certification Program','Space-Talk','Site Visit','Hackathon')),
    CHECK (sponsored IN ('Yes','No') OR sponsored IS NULL),
    CHECK (role IN ('Coordinator','Convenor','Member','') OR role IS NULL),
    CHECK (validation_status IN ('new','valid','invalid','updated'))
);
