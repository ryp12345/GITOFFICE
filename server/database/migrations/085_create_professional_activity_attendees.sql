-- Migration for professional_activity_attendees table
CREATE TABLE professional_activity_attendees (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    egov_id varchar(30) NOT NULL,
    title varchar(255) NOT NULL,
    organizer varchar(255) NOT NULL,
    role text NOT NULL DEFAULT 'Participant',
    level text NOT NULL,
    category text NOT NULL,
    sponsored text,
    sponsored_by varchar(255),
    from_date date NOT NULL,
    to_date date NOT NULL,
    no_of_days integer NOT NULL,
    validation_status text NOT NULL DEFAULT 'new',
    reason varchar(255),
    document varchar(255) NOT NULL,
    created_at timestamp,
    updated_at timestamp,
    CHECK (role IN ('Participant','Resource Person','Jury')),
    CHECK (level IN ('Local','National','International')),
    CHECK (category IN ('Workshop','FDP','Seminar','Webinar','Certification Program','MDP/EDP','Hackathon','Space-Talk','Site Visit','STTP')),
    CHECK (sponsored IN ('Yes','No') OR sponsored IS NULL),
    CHECK (validation_status IN ('new','valid','invalid','updated'))
);
