-- Migration for staff table
CREATE TABLE staff (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id bigint NOT NULL,
    fname varchar(30) NOT NULL,
    mname varchar(30),
    lname varchar(30) NOT NULL,
    local_address text NOT NULL,
    permanent_address text NOT NULL,
    dob date NOT NULL,
    doj date NOT NULL,
    religion_id bigint NOT NULL,
    castecategory_id bigint NOT NULL,
    gender text NOT NULL,
    date_of_increment date,
    date_of_superanuation date NOT NULL,
    date_of_confirmation date,
    bloodgroup varchar(5),
    pan_card varchar(20),
    adhar_card varchar(20),
    contactno bigint,
    aicte_id varchar(30),
    vtu_id varchar(30),
    esi_no varchar(30),
    un_no varchar(30),
    emergency_no numeric(21,0),
    emergency_name varchar(30),
    employeecode integer NOT NULL DEFAULT 0,
    profile_photo varchar(255),
    created_at timestamp,
    updated_at timestamp,
    pf varchar(100),
    CHECK (gender IN ('female','male','others'))
);
