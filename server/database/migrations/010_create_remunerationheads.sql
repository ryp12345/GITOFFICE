CREATE TABLE IF NOT EXISTS remunerationheads (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  remuneration_head VARCHAR(255) NOT NULL,
  created_at TIMESTAMP NULL,
  updated_at TIMESTAMP NULL
);