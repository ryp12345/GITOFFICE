-- Migration for funded_projects table
CREATE TABLE funded_projects (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    egov_id varchar(30) NOT NULL,
    staff_id bigint NOT NULL,
    proposal_title varchar(255) NOT NULL,
    role text NOT NULL,
    type text NOT NULL,
    amount double precision NOT NULL CHECK (amount >= 0),
    proposal_status text NOT NULL,
    application_date date NOT NULL,
    fund_received double precision CHECK (fund_received >= 0),
    project_status text,
    completion_year smallint CHECK (completion_year BETWEEN 1900 AND 9999),
    validation_status text NOT NULL DEFAULT 'new',
    reason varchar(255),
    document varchar(255) NOT NULL,
    created_at timestamp,
    updated_at timestamp,
    CHECK (role IN ('Principle Investigator','Co-Investigator','Not-Applicable','Architect')),
    CHECK (type IN ('Govt-funded','Private funded')),
    CHECK (proposal_status IN ('Accepted','Pending','Rejected')),
    CHECK (project_status IN ('On-Going','Completed') OR project_status IS NULL),
    CHECK (validation_status IN ('new','valid','invalid','updated'))
);
