-- Migration for professor_applications table
CREATE TABLE professor_applications (
    id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    advertisement_instance date,
    application_no varchar(255),
    applicant_name varchar(150) NOT NULL,
    email varchar(255) NOT NULL,
    applicant_address varchar(255),
    applicant_phone bigint,
    phd_university varchar(255),
    phd_reputed_university boolean,
    ug_class text,
    ug_branches varchar(255),
    pg_class text,
    pg_specialization varchar(255),
    ug_pg_remarks varchar(255),
    type text,
    designation text,
    industry_designation varchar(255),
    from_date_asso_prof date,
    to_date_asso_prof date,
    experience_teaching varchar(255),
    experience_research varchar(255),
    experience_industry varchar(255),
    experience_years varchar(255),
    experience_remarks varchar(255),
    post_asso_prof_experience varchar(255),
    asso_prof_experience_remarks varchar(255),
    associate_level varchar(255),
    research_papers_count integer,
    papers_in_sci integer,
    papers_in_ugc integer,
    papers_in_aicte integer,
    research_scholars_count integer,
    research_remarks varchar(255),
    remarks varchar(255),
    publication varchar(255),
    date_of_birth date,
    caste_name varchar(255) NOT NULL,
    eligibility_status text,
    department_id bigint NOT NULL,
    created_at timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CHECK (ug_class IN ('F','S','T') OR ug_class IS NULL),
    CHECK (pg_class IN ('F','S','T') OR pg_class IS NULL),
    CHECK (type IN ('Education','Industry','','') OR type IS NULL),
    CHECK (designation IN ('Assistant Professor','Associate Professor','Professor','') OR designation IS NULL),
    CHECK (eligibility_status IN ('Eligible','Not Eligible') OR eligibility_status IS NULL)
);
