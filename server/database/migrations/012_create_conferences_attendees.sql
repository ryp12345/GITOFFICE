-- Migration for conferences_attendees table
CREATE TABLE conferences_attendees (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    egov_id varchar(30) NOT NULL,
    conference_name varchar(255) NOT NULL,
    attended_as text NOT NULL,
    from_date date NOT NULL,
    to_date date NOT NULL,
    no_of_days varchar(255) NOT NULL,
    title varchar(255) NOT NULL,
    place varchar(255) NOT NULL,
    sponsored text,
    sponsored_by varchar(255),
    amount integer,
    weblink varchar(255),
    type_of_level text NOT NULL,
    issn_no varchar(255),
    validation_status text NOT NULL DEFAULT 'new',
    reason varchar(225),
    document varchar(255) NOT NULL,
    created_at timestamp,
    updated_at timestamp,
    CHECK (attended_as IN ('Resource Person','Participant','Paper Presenter','Session Chair')),
    CHECK (sponsored IN ('Yes','No') OR sponsored IS NULL),
    CHECK (type_of_level IN ('National','International')),
    CHECK (validation_status IN ('new','valid','invalid','updated'))
);
