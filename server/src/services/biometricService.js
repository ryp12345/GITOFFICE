/**
 * Skeleton biometricService.
 * fill in `findMissingPunches` to query real biometric data source.
 */

async function findMissingPunches(date) {
  // Placeholder: return empty array; implement lookup against real biometric table/service.
  return [];
}

module.exports = {
  findMissingPunches,
};
